# Branch Timeline Handoff

## Goal

把旧网页时间轴迁成直接读写 Vault 原生格式的 Obsidian 插件。

## Current

- 插件显示名：`Branch Timeline`；ID：`branch-timeline-hz`，已核实未与社区插件列表重名。
- 插件直接位于 `.obsidian/plugins/branch-timeline-hz/`，桌面与移动端均可加载。
- ItemView 已从简化列表重建为主线/左右分支画布，按重叠区间自动分配车道。
- 已支持双击/移动端双点添加代办、长按添加分支、事项二维拖动、事实起止拉伸、分支起止与横向调整、滚轮/双指/按钮缩放。
- 缩放改为手势中连续预览、手势结束后只提交一次布局；卡片宽度、圆角、文字与摘要连续过渡，不再跨阈值闪烁或突变。
- 缩小时事项连续降级为点/线与外侧标题摘要；事实时长只保留一处，起止时间随缩放隐藏。
- 代办与无时长事实均可从事项菜单开始计时；代办完成后以开始/结束时间转为事实，事实可停止或继续计时，运行态每分钟刷新。
- 顶部恢复 `今天 / 项目 / 习惯 / 成就 / 锚点` 页面导航；今日页不再显示习惯药丸。
- 项目页提供按状态分组的项目卡片；点击卡片进入插件原生项目详情，不再跳到 Markdown。
- 项目详情是连续多日纵向时间轴，进入时聚焦当前日期；缩放刻度会在日、ISO 周、月、年之间切换。
- 项目详情支持双击/双点添加代办、长按空白添加分支、事项只横向换分支、分支起止/横移/换侧/合并/删除，以及事项里程碑旗标。
- 项目分支保存在独立 `projects` 状态中，不复用每日分支；删除项目分支只让事项回主线，不删除事项。
- 页面层已解耦：`pages/navigation.ts` 管导航，`pages/projects.ts` 管项目总览，`pages/project-model.ts` 管项目坐标，`pages/project-detail.ts` 管渲染与手势，`pages/project-actions.ts` 管写入与菜单，`pages/habits.ts` 管习惯周视图；`timeline-view.ts` 只负责页面编排与今日时间线。
- 命令已支持习惯打卡、项目工时、分类时长、项目待办。
- 周记格式：`20_self/22-diary/YY_WN.md` + `## Day_YY-MM-DD`。
- 项目格式：`type: project`；工时写入 `## log`，待办写入 `## 任务` 并附稳定 block ID。
- 插件独有状态：`99_assets/branch-timeline/state.json`。
- 首页分类统计已兼容旧 `[N]` 与新 `[+N]`，按日、分类聚合。
- 标签改为可持久化的对象列表，支持新增、重命名、周记键映射、改色和删除；旧 `tagMap` 自动迁移，删除后不会被默认值恢复。
- 标签设置只展示一个名称输入、颜色和删除；稳定 ID 与旧周记键均隐藏在内部，新标签以稳定 ID 写入分类统计。
- 新时间记录同时保存稳定 `tagId` 与名称快照；标签改名会更新显示，删除标签不删除历史记录。
- 凌晨 02:00 前统一归入前一天，命令入口与主页共用同一日期函数。
- `npm run check`、`npm run build` 通过，12/12 测试通过；已在 Obsidian 1.13.7 实机验证此前的空状态、自动聚焦、缩放控件与双击添加，未写入测试数据。本轮项目详情需重载插件后实机复查。

## Next

1. 安全重载插件后，用真实项目实测项目详情的双击/双点、长按、缩放、事项换分支、分支起止/横移/合并与里程碑菜单。
2. 迁移习惯热力图、睡眠统计、成就和锚点树；成就与锚点当前只有页面入口。
3. 迁移精力背景与完整节律节点，再替换剩余居中选择窗口。

## Do not

- 不迁移或覆盖旧网页 `data/*.json`。
- 不猜测专注、摸鱼、休息的日记分类映射。
- 不把 Site/D1 当作当前数据源。
